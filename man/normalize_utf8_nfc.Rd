% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-download.R
\name{normalize_utf8_nfc}
\alias{normalize_utf8_nfc}
\title{Normalize character columns to UTF-8 NFC}
\usage{
normalize_utf8_nfc(df)
}
\arguments{
\item{df}{A data frame.}
}
\value{
The data frame with character columns normalized; non-character
columns are returned unchanged.
}
\description{
Ensures every character column in a data frame is valid UTF-8 in
canonical NFC form. Without this step, equality comparisons against
string literals fail silently on Windows when the source file's
encoding produces decomposed (NFD) or otherwise non-canonical strings
(e.g. \code{filter(rede == "Pública")} returns 0 rows even though
\code{"Pública"} is in the data). Applied at every read entrypoint —
\code{\link[=read_inep_file]{read_inep_file()}}, \code{\link[=read_ideb_excel]{read_ideb_excel()}}, \code{\link[=read_excel_safe]{read_excel_safe()}}, and the
FUNDEB OData fetcher — so downstream user code can compare with
literals safely on any platform.
}
\keyword{internal}
