# Standardize column names

Internal function to standardize column names to lowercase with
underscores.

## Usage

``` r
standardize_names(df)
```

## Arguments

- df:

  A data frame.

## Value

The data frame with standardized names.
